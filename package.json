{
  "name": "spin-the-wheel",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "preview": "npm run build && npx --yes serve dist"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
